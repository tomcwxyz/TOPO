{
  "name": "topo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*",
    "adapters/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @topo/core && npm run build --workspace @topo/store",
    "check": "npm run check --workspace @topo/core && npm run build --workspace @topo/core && npm run check --workspace @topo/store",
    "test": "npm run build && node --test packages/core/test/*.test.mjs packages/store/test/*.test.mjs",
    "validate": "npm run check && npm test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
