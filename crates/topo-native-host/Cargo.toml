[package]
name = "topo-native-host"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
topo-contracts = { path = "../topo-contracts" }

[dev-dependencies]
tempfile = "3"
